--- a/deploy/kubernetes/base/services/account-service.yaml
+++ b/deploy/kubernetes/base/services/account-service.yaml
@@ -181,8 +181,8 @@
             # business-logic attacks at runtime.
             # Docs: https://docs.datadoghq.com/security/application_security/
             #
-            # - name: DD_APPSEC_ENABLED
-            #   value: "true"
+            - name: DD_APPSEC_ENABLED
+              value: "true"
             # ─────────────────────────────────────────────────────────────────
             # ── DATA STREAMS MONITORING (DSM) ───────────────────────────────
             # End-to-end JMS pipeline visibility (producer → consumer latency,
