# =============================================================================
# Finance Sample App — transaction-service (Node.js / Express)
# =============================================================================
# Payment initiation and ledger write service. Responsibilities:
#   - Accepts payment initiation requests from gateway-api
#   - Writes to the ledger (via account-service or direct DB)
#   - Produces JMS messages to ActiveMQ via STOMP
#
# Health endpoint: GET /health  →  200 OK
# Main API:        POST /v1/payments
# =============================================================================

# ── Service ───────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: finance
  labels:
    app: transaction-service
    component: service
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082

---
# ── Deployment ────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: finance
  labels:
    app: transaction-service
    component: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      annotations:
        # Datadog single-step instrumentation — inject js tracer at pod startup.
        # Pinned to the tracer's MAJOR version (Datadog publishes floating vN
        # init-image tags). More reproducible than "latest" (no surprise major
        # bumps) while still receiving patches; always resolvable — unlike exact
        # patch tags, which don't reliably exist as init-image tags.
        # ── DATADOG INSTRUMENTATION (Single Step library version) ────────
        # Uncomment via 'make instrument' together with the
        # admission.datadoghq.com/enabled label below — tells the Admission
        # Controller webhook which tracer library to inject into this pod.
        # Docs: https://docs.datadoghq.com/tracing/trace_collection/automatic_instrumentation/single-step-apm/
        #
        # admission.datadoghq.com/js-lib.version: "v5"
        # ─────────────────────────────────────────────────────────────────
        # ── DATADOG LOG COLLECTION (autodiscovery annotation) ────────────
        # Uncomment via 'make tags' to tell the Agent which log source and
        # service to tag this pod's stdout/stderr logs with — required for
        # correct parsing/faceting in Log Management.
        # Docs: https://docs.datadoghq.com/containers/kubernetes/log/
        #
        # ad.datadoghq.com/transaction-service.logs: '[{"source":"nodejs","service":"transaction-service"}]'
        # ─────────────────────────────────────────────────────────────────
      labels:
        app: transaction-service
        component: service
        # ── DATADOG INSTRUMENTATION (Single Step Instrumentation) ────────
        # Uncomment via 'make instrument' to opt this pod into the Admission
        # Controller webhook — injects the Datadog tracer at pod startup
        # (APM traces, log-trace correlation, runtime metrics; no code
        # changes, no rebuild required).
        # Docs: https://docs.datadoghq.com/tracing/trace_collection/automatic_instrumentation/single-step-apm/
        #
        # admission.datadoghq.com/enabled: "true"
        # ─────────────────────────────────────────────────────────────────
        # ── DATADOG UST (Unified Service Tagging) ────────────────────────
        # Uncomment via 'make tags' to set the tags.datadoghq.com/* pod
        # labels used for Deployment Tracking / trace-log-metric correlation.
        # Docs: https://docs.datadoghq.com/getting_started/tagging/unified_service_tagging/
        #
        # tags.datadoghq.com/env: "staging"
        # tags.datadoghq.com/service: "transaction-service"
        # tags.datadoghq.com/version: "latest"
        # ─────────────────────────────────────────────────────────────────
    spec:
      # transaction-service writes to the ledger DB (PostgreSQL) and publishes
      # fraud-scoring events via ActiveMQ STOMP. Both init containers prevent
      # startup failures while their respective dependency is initialising.
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            [
              "sh",
              "-c",
              'until nc -z postgres-ledger 5432; do echo "waiting for postgres..."; sleep 3; done',
            ]
        - name: wait-for-activemq
          image: busybox:1.36
          command:
            [
              "sh",
              "-c",
              'until nc -z activemq-artemis 61613; do echo "waiting for activemq stomp..."; sleep 3; done',
            ]
      containers:
        - name: transaction-service
          image: finance-sample-app-transaction-service:latest
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 8082

          env:
            # ── PostgreSQL ──────────────────────────────────────────────
            - name: POSTGRES_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_URL
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: postgres-password
            # ── ActiveMQ (STOMP) ──────────────────────────────────────
            - name: ACTIVEMQ_STOMP_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ACTIVEMQ_STOMP_URL
            - name: ACTIVEMQ_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: artemis-user
            - name: ACTIVEMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: artemis-password
            # ── Upstream services ────────────────────────────────────────────
            - name: ACCOUNT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ACCOUNT_SERVICE_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: REDIS_URL

            # ── DATADOG INSTRUMENTATION (Unified Service Tagging) ───────────
            # Uncomment via 'make tags' to set DD_ENV/DD_SERVICE/DD_VERSION —
            # required for APM/logs/metrics correlation and Deployment
            # Tracking. Docs: https://docs.datadoghq.com/getting_started/tagging/unified_service_tagging/
            #
            # - name: DD_ENV
            #   value: "staging"
            # - name: DD_SERVICE
            #   value: "transaction-service"
            # - name: DD_VERSION
            #   value: "latest"
            # ─────────────────────────────────────────────────────────────────
            # DD_AGENT_HOST uses the node's IP so the pod sends traces/metrics
            # to the DaemonSet agent running on the same node.
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            # ── DATADOG INSTRUMENTATION (Application Security Management) ───
            # Uncomment via 'make security' to enable AppSec threat detection
            # in the tracer. Works alongside the Agent-side asm.threats
            # feature in datadog-agent.yaml. Detects SQLi, XSS, SSRF and
            # business-logic attacks at runtime.
            # Docs: https://docs.datadoghq.com/security/application_security/
            #
            # - name: DD_APPSEC_ENABLED
            #   value: "true"
            # ─────────────────────────────────────────────────────────────────
            # ── DATA STREAMS MONITORING (DSM) ───────────────────────────────
            # Uncomment via 'make instrument' to enable JMS pipeline
            # visibility in Data Streams. This service produces to ActiveMQ;
            # full producer→consumer stitching across the Python/Go consumers
            # may need manual checkpoints (see docs).
            # Docs: https://docs.datadoghq.com/data_streams/
            #
            # - name: DD_DATA_STREAMS_ENABLED
            #   value: "true"
            # ─────────────────────────────────────────────────────────────────
            # ── CONTINUOUS PROFILER ──────────────────────────────────────────
            # Uncomment via 'make instrument' to enable Continuous Profiler for
            # this service — correlate CPU flame graphs with slow traces.
            # Single Step Instrumentation already injects dd-trace for Node.js
            # APM; dd-trace's profiler is enabled by this plain env var, no
            # code changes needed.
            # Docs: https://docs.datadoghq.com/profiler/
            #
            # - name: DD_PROFILING_ENABLED
            #   value: "true"
            # ─────────────────────────────────────────────────────────────────
          # ── Health checks ──────────────────────────────────────────────────
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
