--- a/deploy/kubernetes/base/services/fraud-detection.yaml
+++ b/deploy/kubernetes/base/services/fraud-detection.yaml
@@ -135,8 +135,8 @@
             # business-logic attacks at runtime.
             # Docs: https://docs.datadoghq.com/security/application_security/
             #
-            # - name: DD_APPSEC_ENABLED
-            #   value: "true"
+            - name: DD_APPSEC_ENABLED
+              value: "true"
             # ─────────────────────────────────────────────────────────────────
             # ── DATA STREAMS MONITORING (DSM) ───────────────────────────────
             # JMS consumer on fraud.score.queue. Enabling the flag turns DSM on;
